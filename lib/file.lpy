(define outer [x]
    (do 
        (define inner [x]
            (x)
        )
        (inner 5)
        (x)
    )
)

(outer 3)
(outer 3)





(def fib [n] (if (= n 0) 1 2))

(define outer [x] (do (define inner [x] (+ x 2 )) (inner 2)))